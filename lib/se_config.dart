// Default SE identifier for the demo. Tags all Sentry events for separation.
// Keep this as 'tda' in the repo — do NOT commit your own name. If you need to
// separate your own events locally, change it locally but don't push that change.
const String se = 'tda';
